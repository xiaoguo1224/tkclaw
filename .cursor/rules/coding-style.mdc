---
description: 项目编码风格约束
globs: 
alwaysApply: true
---

# 编码风格

## UX / UI 质量优先

**UX 和 UI 对产品至关重要。每一次涉及前端的改动，都必须从用户体验角度审视，不能只做"功能实现"就收工。**

### 必须做

- **错误提示必须可操作**：用户看到错误后，必须知道下一步该做什么。错误文案要包含具体的操作指引（如"请前往系统设置配置 xxx"），能加跳转链接就加跳转链接
- **空状态必须有引导**：列表为空、配置缺失、权限不足等场景，必须告诉用户"为什么"和"怎么办"，不能只显示一个空白页面
- **操作反馈必须即时**：按钮点击后要有 loading 状态，异步操作要有进度或结果提示，不能让用户猜"到底有没有成功"
- **每个改动都要自问**：如果我是第一次用这个产品的用户，看到这个界面/提示/报错，我能立刻知道该怎么做吗？不能就说明 UX 不合格
- **主动发现 UX 缺陷**：在实现功能的过程中如果发现现有 UX 有问题（如缺少引导、报错不明确、操作路径不清晰），主动报告给用户并建议改进

### 严禁

- **禁止"功能做完就完了"**：功能实现只是及格线，用户体验才是目标
- **禁止忽略边缘场景的 UX**：错误状态、加载状态、空状态、权限不足状态，都是用户会遇到的真实场景，不能只关注 happy path
- **禁止把 UX 优化当"锦上添花"**：UX 问题就是 bug，不是"有空再说"的低优先级事项

## Emoji 使用规则

**未经用户明确允许，禁止在任何地方使用 emoji。** 包括但不限于：

- Vue 模板中的图标 — 使用 Lucide Vue Next 图标组件（`lucide-vue-next`）
- 提示文字、占位符、label — 纯文本
- 日志输出、Toast 消息 — 纯文本
- HTML 实体字符当图标（如 ◫ ☐ ⚙ ⌘）— 同样禁止，用 Lucide 组件

```vue
<!-- 禁止 -->
<span>🔍 搜索实例...</span>
<span>🔔</span>
<span>{{ '◫' }}</span>

<!-- 正确 -->
<Search class="w-4 h-4" />
<Bell class="w-4 h-4" />
<LayoutGrid class="w-4 h-4" />
```

## 图标规范

项目统一使用 `lucide-vue-next`，按需导入：

```vue
<script setup lang="ts">
import { Search, Bell, Settings } from 'lucide-vue-next'
</script>
```

常用尺寸：`w-4 h-4`（导航/按钮）、`w-5 h-5`（Logo/标题）。

## 语言表达

**与用户交互时，所有专业术语、变量名、配置项后面必须跟中文说明。** 不要假设用户能直接看懂裸英文术语。

- 配置项/变量名：`IMAGE_REGISTRY`（镜像仓库地址）、`VKE_SUBNET_ID`（VKE 子网 ID）
- 技术概念：SSE（服务端推送）、KubeConfig（集群连接凭证）、Pod（容器组）
- 选项/问题中出现的术语同样需要加中文标注

**禁止在提问或说明中堆砌纯英文术语来"装专业"。**

## 回答准则

**三思而后行，不要一拍脑袋就回答。想当然 = 事故。**

核心原则：**不确定的事情，先查证再开口。查不到就说查不到，绝不编。**

**证据优先原则：任何涉及运行状态、系统行为、错误原因的结论，必须基于工具获取的第一手证据（读文件、跑命令、查日志、搜代码），不允许仅凭对话上下文或代码阅读推断。** 上下文只能用来缩小排查范围，不能用来得出结论。

"我看了代码觉得应该是这样" ≠ 证据。"我跑了命令/读了日志/查了状态，实际结果是这样" = 证据。

### 必须做

- **知道就是知道**：明确说明依据（看了哪个文件、哪行代码、哪条日志、哪个命令的输出）
- **不知道就是不知道**：列出你做了哪些信息获取（读了什么文件、跑了什么命令、搜了什么关键词），最终为什么仍然不确定
- **先查再答**：涉及项目状态的问题（端口是否在用、进程是否在跑、文件是否存在），先用工具验证，不要凭记忆回答
- **先读代码再写代码**：涉及第三方项目/依赖的行为（如 DeskClaw 的认证方式、API 用法），必须先读源码确认，不要凭"常见做法"臆断。写提示信息、文档、注释前，确认描述的行为与代码一致
- **出错就认**：说错了直接承认，不要找借口圆
- **结论必须附证据来源**：每个关键结论必须标注依据（"读了 xxx 文件第 N 行" / "跑了 xxx 命令输出为 xxx" / "查了 xxx 日志看到 xxx"）。没有证据来源的结论不允许输出
- **工具调用优先于推理**：能用工具验证的事情，不要用推理代替。即使你"很确定"，也必须跑一遍工具确认

### 严禁

- **禁止仅凭对话上下文下结论**：用户描述的症状、之前的代码改动、对话中提到的信息，只能作为排查线索，不能直接作为结论依据。必须用工具去验证这些线索是否成立
- **禁止"我看了代码所以我知道"**：代码逻辑和运行时行为是两回事。看了代码只能说"代码逻辑是这样"，不能说"系统行为是这样"——后者必须通过实际运行/日志/状态来验证
- **禁止猜测性断言**：不确定的事情用"我不确定"、"需要进一步确认"等措辞，不要伪装成确定的结论
- **禁止想当然**：不要因为"一般项目都这样"就假设当前项目也一样。每个结论都要有当前项目的代码/配置/日志作为依据
- **禁止半吊子回答**：如果只查了一半就急着回答，宁可多花时间查完整。回答一次就要回答对，不要让用户反复纠正
- **禁止信息编造**：没看到的文件内容、没读过的源码行为、没验证过的命令输出，一律不准当作事实陈述
- **禁止脱离上下文排查**：排查问题时必须联系对话上下文。用户刚改过 DNS/IP/配置？先考虑缓存、生效延迟等基本因素，不要跳到"安全组"、"网络不通"等高级猜测。排查顺序：缓存/生效 -> 配置正确性 -> 连通性 -> 安全策略
- **禁止预设用户行为**：不要用猜测性假设来推断用户做了什么或没做什么（如"你需要重启服务"、"你可能没保存文件"）。必须先通过工具查看实际状态（终端输出、进程列表、日志、文件内容），用第一手证据确认真实情况后再给出判断。用户报告问题时，默认相信用户的描述，从实际状态出发排查，而不是质疑用户的操作

### 回答流程

1. 收到问题后，先判断：我是否有足够的依据回答？
2. 如果没有 → 先用工具查证（读文件、搜代码、跑命令）
3. 查证完毕后，组织回答，每个关键结论附上依据来源
4. 回答前自检：有没有哪句话是我"觉得应该是这样"但其实没验证过的？有就去验证或删掉

## 问题处理流程

**发现问题后不要立即动手修，先报告给用户，等用户确认方案后再改。**

用户可能有更好的方案，擅自修改可能导致返工甚至引入新问题。

### 流程

1. **发现问题** → 明确描述问题是什么、影响范围、根因分析
2. **提出方案** → 给出你的建议修复方案（可以多个），说明各自优缺点
3. **等待确认** → 用户确认后再动手改代码
4. **执行修复** → 按确认的方案执行

### 例外（可以直接修）

- 明显的拼写错误、导入缺失等不影响逻辑的小修复
- 用户明确说"你直接修"或"帮我搞定"时
- lint 错误等纯格式问题

### 严禁

- **禁止自作主张**：发现问题就直接改，不告知用户
- **禁止边分析边改**：还没搞清楚根因就急着写代码修
- **禁止假设方案唯一**：总是考虑用户可能有更好的想法
- **禁止"分析完顺手就改"**：即使已经定位了根因、方案也很明确，仍然必须先报告并等用户说"改"才动手。"我直接改了"这种话不应出现
- **禁止把"用户让我排查"等同于"用户让我修"**：排查 = 查原因并汇报，不等于授权修改代码

## 破坏性操作必须逐项确认

**以下操作属于"破坏性操作"，执行前必须逐项列出并获得用户明确确认，即使用户已经确认了总体计划。**

"确认了计划" ≠ "授权了每一步破坏性操作"。计划是方向，执行时每个不可逆操作都要单独确认。

### 必须逐项确认的操作

- **K8s 资源删除**：`kubectl delete namespace`、`kubectl delete deployment`、`kubectl delete ingress`、`kubectl delete service`、`kubectl delete secret` 等
- **K8s 资源替换**：删除后重建 IngressClass、StorageClass 等不可变资源
- **数据库操作**：`DROP DATABASE`、`ALTER DATABASE RENAME`、`DELETE FROM`、`TRUNCATE`
- **DNS / 域名变更**：修改 Ingress host、删除 ALB 路由规则
- **Docker 镜像删除**：删除远程仓库中的镜像 tag
- **NAS / 存储路径变更**：重命名或删除 NFS 目录、修改 StorageClass subPath
- **Git 历史操作**：`git push --force`、`git rebase`、`git reset --hard`

### 确认格式

执行前必须以清单形式列出即将执行的破坏性操作，等用户逐条确认：

```
即将执行以下破坏性操作，请确认：
1. 删除 IngressClass nginx（会影响所有 nginx class Ingress）
2. 删除 nodeskclaw-system namespace（包含其中所有资源）
3. ...

确认后我再执行。
```

### 严禁

- **禁止连锁执行**：不能因为用户确认了"删除旧 namespace"就顺带把 IngressClass、ALB Ingress 都删了
- **禁止在 AskQuestion 选项中隐含破坏性操作**：选项描述必须明确告知会删除什么
- **禁止"先删再建"的乐观假设**：删除和创建之间如果有失败风险，必须先告知用户可能的中断时间

### 例外（可以不单独确认）

- 用户明确说"直接执行"、"全部按计划来"、"不用问我"时
- 在测试环境中用户已声明可以随意操作时
- 创建操作（不涉及删除、修改现有资源）

## K8s / DeskClaw 排查规则

**用户提到的任何 DeskClaw 或 K8s 相关问题，必须先通过 kubectl 实际查看集群状态，再做判断。** 不允许凭代码逻辑或经验猜测。

### DeskClaw 行为判断必须有源码依据

**对 DeskClaw 内部行为（skill 加载、session 管理、system prompt 构建、配置生效机制等）的任何判断，必须通过读取实际源码来验证。** 不允许凭"可能是这样"、"一般框架都这么做"等推测下结论。

- **必须做**：读取 DeskClaw 源码，找到具体函数和文件作为依据
- **必须做**：涉及多个模块的交互时，追踪完整调用链（如 gateway → skills → pi-coding-agent → system-prompt），不能只看一层就下结论
- **必须做**：结论中明确引用源码位置（如 "`src/agents/system-prompt.ts` 中的 `buildSkillsSection` 函数"）
- **严禁**：没读过 DeskClaw 源码就断言"这属于 DeskClaw 自身的行为"、"不是我们的 bug"
- **严禁**：基于 UI 表现推测内部机制（如看到 Skills 页面显示 enabled 就断言 agent 一定能用）
- **严禁**：只查了一层就下结论（如只看 `loadSkillsFromDir` 返回了 skill 就说"skill 已加载"，必须追到 system prompt 层确认 skill 是否真的在 prompt 中）

### 源码获取方式

**本项目在 `/Users/xy718/workspace/NoDeskClaw/openclaw/` 有 DeskClaw 的完整 TypeScript 源码副本。** 优先使用本地源码，可读性远优于 Pod 上的 minified dist 文件。

按优先级：

1. **本地源码（首选）**：直接读取 `openclaw/src/` 下的 TypeScript 源文件。用 Grep/SemanticSearch 搜索关键词定位函数
2. **Pod 上验证**：需要确认运行时实际状态（如 session store 内容、config 文件）时，通过 `kubectl exec` 查看
3. **Pod 上读 dist（备用）**：本地源码版本与线上不一致时，通过 `kubectl exec <pod> -- node -e "..."` 读取 dist 文件

### 常用 DeskClaw 源码路径

| 模块 | 本地路径（`openclaw/src/`） | Pod 内 dist 路径 |
|---|---|---|
| Skill 加载与过滤 | `agents/skills.ts` | `dist/skills-*.js` |
| System Prompt 构建 | `agents/system-prompt.ts` | `dist/extensionAPI.js` |
| Agent Session / 嵌入式运行 | `agents/pi-embedded-runner.ts` | `dist/extensionAPI.js` |
| Session 消息处理 / Reply | `auto-reply/` | `dist/reply-*.js` |
| Skill 快照与版本 | `agents/skills.ts`（`getSkillsSnapshotVersion`、`ensureSkillsWatcher`） | `dist/extensionAPI.js` |
| Gateway 路由 | `gateway/` | `dist/gateway-cli-*.js` |
| 配置加载 | `config/` | `dist/config-*.js` |
| Session Store（运行时） | — | `/root/.openclaw/agents/main/sessions/sessions.json` |

> 注：`pi-coding-agent` 是 DeskClaw 的闭源 NPM 依赖，源码不在本地。相关逻辑（`loadSkillsFromDir`、`formatSkillsForPrompt`、`ResourceLoader`、`PackageManager`、以及 `systemSent` 的读取逻辑）需通过 Pod 上的 `node_modules/@mariozechner/pi-coding-agent/dist/core/` 读取。**搜不到不等于不存在**——DeskClaw 开源代码可能仅定义了类型（如 `SessionEntry.systemSent`），而运行时逻辑在 pi-coding-agent 中。判断某字段"被忽略"前，必须同时搜索开源代码和 dist 文件，或通过实际行为验证（如修改字段后观察 Pod 行为变化）。

### 必查项

用户反馈 DeskClaw 实例问题时，按以下顺序执行：

1. **Pod 状态**：`kubectl get pods -n <namespace>` — 确认 Running / CrashLoopBackOff / Pending 等
2. **Pod 详情**：`kubectl describe pod <pod> -n <namespace>` — 查看 Events、Conditions、Container State
3. **Pod 日志**：`kubectl logs <pod> -n <namespace> --tail=30` — 查看 DeskClaw 启动日志和错误输出
4. **Deployment 状态**：`kubectl get deploy -n <namespace>` — 确认 READY、UP-TO-DATE、AVAILABLE
5. **配置文件**：`kubectl exec <pod> -- cat /root/.openclaw/openclaw.json` — 确认实际运行配置
6. **环境变量**：`kubectl exec <pod> -- printenv | grep OPENCLAW` — 确认 token 等关键变量
7. **Events**：`kubectl get events -n <namespace> --sort-by='.lastTimestamp'` — 查看最近集群事件

### 什么算"DeskClaw 或 K8s 相关"

- 实例打不开、502、503、连接失败
- pairing required、token mismatch、disconnected
- 实例状态异常（一直部署中、重启失败、运行中但不可访问）
- Pod/容器/Deployment/Service/Ingress 相关的任何问题
- NFS 挂载、PVC、配置文件读写问题

### 严禁

- **禁止不查就猜**：不跑 kubectl 就说"应该是 xxx 问题"
- **禁止只看代码**：代码逻辑正确不代表集群状态正确，必须用 kubectl 拿到第一手证据
- **禁止省略关键步骤**：不要只查 Pod 状态就下结论，日志和 Events 同样重要

## NFS 路径映射规则

**通过 NFS 挂载操作 Pod 文件时，从文件内容中读出的路径都是容器视角的绝对路径，不能直接在本地使用。**

这个问题已经发生过：`sessions.json` 的 `sessionFile` 存的是 `/root/.openclaw/agents/main/sessions/xxx.jsonl`（Pod 内路径），代码直接 `Path(session_file).exists()` 检查，在本地必然返回 `False`，导致进化通知注入被静默跳过。

### 必须做

- **读到路径字段就问"这是谁的视角"**：容器的、NFS 挂载的、还是本地的？
- **容器路径必须转换**：从 NFS 文件中读出的绝对路径（如 `/root/.openclaw/xxx`）必须转换为本地 NFS 挂载路径后使用
- **转换模式**：`local_path = sessions_path.parent / Path(container_path).name`（同目录文件用 name），或 `mount_path / ".openclaw" / container_path.split("/.openclaw/", 1)[-1]`（通用）
- **涉及文件**：`nfs_mount.py`、`gene_service.py` 中所有 `async with nfs_mount(...)` 上下文内的代码

### 严禁

- **禁止直接使用容器路径**：`Path(entry.get("sessionFile")).exists()` 这种写法在本地永远不通
- **禁止静默跳过**：路径不存在时至少要 `logger.warning`，不能 `continue` 了事让 bug 隐形

## 排查问题方法论

**问题没解决就不叫排查完了。修了一刀发现没效果，必须继续深挖到真正的根因。**

### 排查原则

- **端到端验证**：修完后必须验证用户报告的问题是否真的消失了，不能"改了代码就收工"
- **分层排查**：问题涉及多层（前端 → 后端 → K8s → 镜像仓库）时，从最终现象反向逐层验证，每一层都要有实际证据（日志、API 响应、K8s describe/events）
- **不要只修最表层**：用户说"实例一直部署中"，不能只修前端显示逻辑——要一路追到 K8s Pod 的实际状态，看是 Pending/CrashLoopBackOff/ImagePullBackOff 还是真的在 Running

### 必须做

- **拿到第一手证据**：涉及 K8s 部署问题时，必须查 Pod Events、Describe、Container Status，不要凭推测
- **多假设并行排除**：第一个假设修完没效果，立刻列出其他可能原因逐个排查，不要反复修同一层
- **报告排查链路**：告诉用户"我查了 A、B、C 三层，A 层正常（依据：xxx），B 层正常（依据：xxx），C 层发现问题（依据：xxx）"
- **修完要验证**：改了代码后跑一遍完整流程确认修复有效，或者告诉用户需要做什么验证步骤

### 严禁

- **禁止"改了就算修了"**：代码改了但没验证效果 = 没修
- **禁止浅层排查就收工**：只看了后端日志就下结论，没去查 K8s 层、没去查镜像仓库、没去查前端请求，都不算排查完
- **禁止忽略用户反馈**：用户说"还是不行"，必须重新排查，不能坚持"我改了应该没问题"

### 排查顺序参考（部署/运行类问题）

1. **前端**：请求参数是否正确、API 是否返回预期数据
2. **后端**：API 日志、后台任务是否在跑、DB 状态是否一致
3. **K8s**：Pod 状态（`kubectl describe pod`）、Events、Container State
4. **基础设施**：镜像是否存在、镜像架构是否匹配、PVC 是否绑定、网络策略是否放行

## 前置条件检查

**启动任何流程前，必须先检查并列出所有前置条件。缺少任何一个就中断流程、明确告知用户缺什么、怎么补，等用户确认后再继续。**

常见前置条件包括但不限于：

- **环境变量 / 密钥**：启动服务前检查 `.env` 中必要字段是否有值（如 `DATABASE_URL`、`FEISHU_APP_ID`、`JWT_SECRET` 等），空值 = 缺失
- **外部服务依赖**：数据库能否连上、第三方 API 凭证是否配好
- **端口占用**：启动服务前检查目标端口是否被占
- **依赖安装**：`node_modules/` 或 `.venv/` 是否存在、是否需要 `npm install` / `uv sync`

**执行要求**：

1. 在执行操作前，先列出该操作的全部前置条件
2. 逐项检查每个前置条件是否满足
3. 任何一项不满足：**立即停止**，告诉用户缺什么、怎么补
4. 全部满足才继续执行
5. 不要假设用户已经配好了任何东西，不要跳过检查直接干

## Docker 镜像架构规则

**所有 Docker 镜像操作必须显式指定 `linux/amd64` 平台，因为开发机是 Apple Silicon（arm64），而目标 K8s 集群（火山云 VKE）运行 `amd64` 节点。**

这个问题已经反复出现过多次，必须严格遵守。

### 必须做

- `docker build` 必须加 `--platform linux/amd64`
- `docker pull`（用于搬运镜像到私有仓库时）必须加 `--platform linux/amd64`
- 编写 Dockerfile、构建脚本、文档中涉及 `docker build` / `docker pull` 的地方，都要包含平台参数
- 给用户提供 Docker 命令时，始终带上 `--platform linux/amd64`

### 严禁

- **禁止省略 `--platform`**：不要假设默认架构正确，Mac 默认拉 arm64，推到 CR 后 amd64 节点拉不了
- **禁止事后才想起来**：写命令的第一时间就加上，不要等用户踩坑后再补

### 示例

```bash
# 正确
docker pull --platform linux/amd64 registry.k8s.io/ingress-nginx/controller:v1.12.0
docker build --platform linux/amd64 -t my-image:latest .

# 错误 - 缺少 --platform，Mac 上会拉/构建 arm64
docker pull registry.k8s.io/ingress-nginx/controller:v1.12.0
docker build -t my-image:latest .
```

## JSONC 配置文件解析规则

**`openclaw.json` 及从 `openclaw.json.template` 生成的配置文件可能包含 `//` 行注释（JSONC 格式）。标准 `JSON.parse()` 不支持注释，直接解析会报 SyntaxError。**

这个问题已经发生过：entrypoint 中新增的配置补全步骤直接用 `JSON.parse()` 读取包含 `//` 注释的 `openclaw.json`，导致解析失败 → `set -euo pipefail` 使脚本中断 → 容器 CrashLoopBackOff。

### 必须做

- **解析前剥离注释**：任何用 `JSON.parse()` 读取 `openclaw.json` 的地方，必须先 `text.replace(/^\s*\/\/.*$/gm, '')` 剥离行注释
- **写回时用标准 JSON**：`JSON.stringify(obj, null, 2)` 写回的是干净 JSON（无注释），后续解析不会再有问题
- **新增解析逻辑时自查**：写 `JSON.parse` 读取任何可能含注释的配置文件前，先确认来源是否可能是 JSONC

### 涉及场景

| 场景 | 文件 | 风险 |
|------|------|------|
| entrypoint 配置补全 | `docker-entrypoint.sh` 中的 `node -e` | 首次从模板生成的配置含注释 |
| 后端读取 Pod 配置 | `llm_config_service.py` 通过 `kubectl exec cat` 读取 | 如果 Pod 上配置未被 backend 重写过，可能仍含注释 |
| 任何新增的配置迁移/修补脚本 | — | 同上 |

### 严禁

- **禁止裸用 `JSON.parse()` 读取 `openclaw.json`**：必须考虑 JSONC 可能性
- **禁止假设配置文件是干净 JSON**：模板生成的文件、用户手动编辑过的文件都可能含注释

## 基础设施约束

- **数据库**：火山云 RDS PostgreSQL，不用本地 SQLite，不自建 Docker PostgreSQL 容器
- **K8s**：火山云 VKE，M1 单集群，节点架构 `linux/amd64`
- **不准擅自引入新的基础设施依赖**（如本地数据库容器、消息队列、Redis 等），必须先跟用户确认

## 软删除规则

**所有数据删除必须使用逻辑删除，严禁物理删除。**

### 必须做

- 删除操作统一设置 `deleted_at = func.now()`，使用 `BaseModel.soft_delete()` 方法
- 所有数据库查询必须过滤已删除记录：`Model.deleted_at.is_(None)`
- 级联删除场景（如删除集群时其下实例和部署记录）需手动级联设置子记录的 `deleted_at`

### 唯一约束必须用 Partial Unique Index

**所有需要唯一性保证的字段组合，必须使用 `Index(..., unique=True, postgresql_where=text("deleted_at IS NULL"))`，严禁使用 `UniqueConstraint`。**

普通 `UniqueConstraint` 覆盖全表（含软删除记录），导致"删除后重新创建"时 INSERT 触发 `IntegrityError` → 500。Partial Unique Index 只对 `deleted_at IS NULL` 的活跃记录生效，软删除记录不参与唯一性检查。

```python
# 正确
Index("uq_xxx", "col_a", "col_b",
      unique=True, postgresql_where=text("deleted_at IS NULL"))

# 错误 — 会导致软删除后同键位无法新建
UniqueConstraint("col_a", "col_b", name="uq_xxx")
```

### 严禁

- **禁止 `db.delete()`**：不允许调用 SQLAlchemy 的物理删除方法
- **禁止 `DELETE FROM`**：不允许原生 SQL 物理删除
- **禁止遗漏过滤**：新增查询必须包含 `deleted_at.is_(None)` 条件，否则会查到已删除的数据
- **禁止使用 `UniqueConstraint`**：所有唯一约束必须用 Partial Unique Index（见上）

## 导入完整性规则

**在函数/代码块内使用模型或工具类时，必须确保该作用域内有对应的 import。**

- `main.py` 的 `lifespan` 函数中延迟导入了部分模型（如 `Cluster`），但新增的迁移代码使用了其他模型（如 `User`、`Instance`）时，必须在同一作用域内补上 `from app.models.xxx import Xxx`
- 不要假设外层已经导入了某个符号——每个 `async with` 块都是独立作用域，需要各自导入依赖

## 同源逻辑同步修改规则

**修改一处逻辑后，必须主动搜索项目中是否存在相同或相似的逻辑副本，全部同步修改。**

本项目存在多个前端工程（`ee/nodeskclaw-frontend` 管理平台、`nodeskclaw-portal` 用户门户）和共享的后端逻辑，同一功能经常在多处有对应实现。

### 必须做

- **改完立刻搜**：修改完一处后，用关键词（函数名、变量名、算法特征）在整个项目中搜索是否有相同逻辑
- **全部同步改**：找到的所有副本都必须应用相同的修改
- **主动报告**：告知用户"在 X 处也发现了相同逻辑，已同步修改"

### 高频同源位置

| 逻辑类型 | 可能存在的位置 |
|----|----|
| slug 生成、表单校验 | `nodeskclaw-portal` 和 `ee/nodeskclaw-frontend` 的对应页面 |
| API 调用封装 | 两个前端工程各自的 `api.ts` / `composables` |
| 类型定义、常量 | 两个前端工程各自的 `types/`、组件内 |
| K8s 资源构建逻辑 | 后端 `resource_builder.py`、`deploy_service.py` |

### 严禁

- **禁止只改一处就收工**：不搜索就断言"只有这一处"
- **禁止事后补**：不要等用户发现另一处没改再回来补

## Plan 模式提问规则

**Plan 模式下向用户提问时，问题必须有深度、有广度、透彻、精确、有思考、有细节。** 禁止提出敷衍的、表面的、缺乏技术判断的问题。

### 提问前必须做

- **先自行调研**：提问前先读代码、查配置、搜文档，把能自己搞清楚的都搞清楚。只把真正需要用户决策的问题抛出来
- **分析可行性**：每个选项都要先在脑中推演其技术可行性、实现复杂度、对现有架构的影响，再呈现给用户
- **预判边界情况**：除了 happy path，还要考虑异常场景、兼容性、性能、安全、维护成本等
- **带上你的判断**：不要只罗列选项让用户选——给出你的推荐，并说明推荐理由

### 问题质量标准

- **深度**：不停留在"要不要做"这个层面，要深入到"怎么做、有什么坑、对现有系统有什么影响"
- **广度**：考虑关联影响——改了 A 模块，B、C 模块是否受影响？前端、后端、基础设施是否都需要配合？
- **精确**：问题要具体到技术细节——不要问"你想怎么部署"，要问"是用 Nginx default-backend 302 跳转，还是在 Ingress 注解里配置 custom-http-errors 指向自定义错误页面服务"
- **有思考**：展示你的分析过程——"我考虑了 A 方案和 B 方案，A 的优点是 xxx 但缺点是 xxx，B 的优点是 xxx"
- **有细节**：涉及配置、URL、端口、域名、路径等具体值时，直接给出具体例子而非抽象描述

### 严禁

- **禁止懒问**：能自己查到的信息不要问用户（如"这个文件在哪"、"用的什么框架"）
- **禁止空泛提问**：不要问"你想怎么做"这种把思考责任甩给用户的问题
- **禁止不带分析的选择题**：每个选项必须附带优缺点分析，不能只是干巴巴列 A/B/C
- **禁止只问不想**：提问前必须自己先有判断，告诉用户你倾向哪个方案、为什么

## 分发文件清单同步规则

**当项目中存在"文件白名单"机制（如 `PLUGIN_FILES` 列表控制哪些文件被复制/分发到目标环境）时，新增或删除源文件必须同步更新对应的白名单。**

这个问题已经发生过：新增了 `sse-server.ts` 但忘记加到 `PLUGIN_FILES`，导致文件没被复制到实例上，plugin 加载失败。

### 必须做

- 在源目录新增文件后，**立刻检查**是否有对应的文件清单/白名单需要更新
- 删除文件时同步从清单中移除
- 当前已知的文件清单：`nodeskclaw-backend/app/services/llm_config_service.py` 中的 `PLUGIN_FILES`

### 检查流程

1. 向 `openclaw-channel-nodeskclaw/` 添加新 `.ts` 文件
2. 搜索 `PLUGIN_FILES` 关键词
3. 确认新文件已加入列表
4. 如果遗漏，立即补上

## 按钮光标规则

**全局 CSS 已在 `globals.css` 的 `@layer base` 中为 `button:not(:disabled)` 和 `[role="button"]:not(:disabled)` 设置了 `cursor: pointer`。** 两个前端项目（`ee/nodeskclaw-frontend`、`nodeskclaw-portal`）均已配置。

### 禁止

- **禁止给 `<button>` 元素手动加 `cursor-pointer` 类** — 全局规则已覆盖，手动添加纯属冗余
- **禁止在组件 `<style>` 中给 button 写 `cursor: pointer`** — 同上

### 非按钮可点击元素仍需手动处理

全局规则只覆盖 `<button>` 和 `[role="button"]`。以下元素如果可点击，仍需在模板中手动加 `cursor-pointer`：

- 带 `@click` 的 `<div>`、`<span>`、`<li>` 等非语义化按钮元素
- `<select>` 下拉框
- `<label>` 标签（用作切换开关等）
- 带 `@click` 的卡片容器

## 滚动条样式规则

**禁止在组件内自定义滚动条样式。** 全局样式已在 `globals.css` 中统一定义。

### 必须做

- 使用 Tailwind 的 `overflow-y-auto` / `overflow-x-auto` 即可，全局样式自动生效
- 输入框等紧凑场景使用 `.scrollbar-compact` 类
- 需要滚动条始终可见时使用 `overflow-y-scroll`

### 严禁

- 在组件 `<style>` 中写 `::-webkit-scrollbar` 相关样式
- 在组件中写 `scrollbar-width` / `scrollbar-color`
- 使用 Tailwind 的 `scrollbar-thin` 等第三方插件类

## 下拉框规则

**禁止使用原生 `<select>` / `<option>` 元素。** 所有下拉选择必须使用自定义 button-based dropdown。

### 实现模式

```vue
<!-- 触发按钮 -->
<button @click="dropdownOpen = !dropdownOpen">
  选择内容 <ChevronDown />
</button>
<!-- 下拉面板 -->
<div v-if="dropdownOpen" class="absolute z-10 ...">
  <button v-for="item in options" @click="select(item)">
    {{ item.label }}
  </button>
</div>
```

### 原因

- 原生 `<select>` 样式无法自定义，与项目设计风格不一致
- 原生 `<option>` 不支持图标、徽标等富文本内容

### 已知存量

`ee/nodeskclaw-frontend`（管理后台）中有 2 处原生 `<select>`（`Cluster/Detail.vue`、`AdvancedConfigPanel.vue`），后续迭代清理。

## i18n 文案与错误码规则

**新增或修改任何用户可见文案时，必须同步接入 i18n（国际化）词条，不允许新增硬编码中文 UI 文案（专有名词除外）。**

### 前端必须做

- 新文案必须使用 `t('...')` 或等价 i18n 调用，不允许在模板/脚本中直接写中文提示词
- Toast（通知）、弹窗、表单校验、空状态、按钮文案必须统一走词条
- 错误展示优先 `message_key`（文案键）翻译，词条缺失时回退 `message`（文案）
- 请求必须带 `Accept-Language`（语言请求头）

### message_key 命名规范

- 统一使用小写点分层级：`<domain>.<module>.<action_or_reason>`
- 推荐前缀：
  - 错误：`errors.auth.token_invalid`
  - 提示：`notices.instance.restart_submitted`
  - 校验：`validation.instance.name_required`
- 严禁使用中文、空格、驼峰、下划线混搭（如 `Error.TokenInvalid`、`errors_token_invalid`）

### 插值规范

- 一律使用命名参数，不使用位置参数
- 示例：
  - 词条：`errors.instance.not_found = "实例 {name} 不存在"`
  - 调用：`t('errors.instance.not_found', { name })`
- 计数文案统一使用 `count` 作为参数名（如 `{ count }`）

### 后端错误契约约束

- 失败响应必须包含 `error_code`（错误码）+ `message_key`（文案键）+ `message`（文案）
- 只要出现 `error_code` 就视为失败
- 禁止再新增 `detail`（错误详情字段）作为前端展示主路径

### 严禁

- 禁止新增硬编码中文 UI 文案（专有名词和注释除外）
- 禁止新增无语义 `message_key`（如 `msg1`、`error_x`）
- 禁止前端直接拼接未经 i18n 管理的长句错误提示

## 自动化页面测试执行规则

**在用户没有明确提出页面自动化测试需求时，禁止主动执行自动化页面测试（如浏览器自动点击、页面冒烟脚本）。**

### 必须做

- 仅在用户明确要求时执行页面自动化测试（如"帮我做页面冒烟"、"跑一下自动化 UI 验证"）
- 默认优先执行非页面自动化验证（如构建、lint、单元测试、类型检查）
- 如果你判断页面验证有必要，先向用户说明原因并征得确认，再执行自动化页面测试

### 严禁

- 禁止在未获明确需求时自行启动浏览器自动化流程
- 禁止把"顺手验证"当作默认动作执行页面自动化测试

## 部署脚本执行规则

**禁止 AI 助手直接运行 `deploy/cli.sh` 或任何构建/部署命令。** 构建镜像和部署到集群必须由用户手动执行。

### 必须做

- 代码改完后提交 commit，然后告知用户"代码已提交，可以部署了"
- 如果用户需要部署命令，给出完整的可复制命令，让用户自己粘贴运行
- 部署相关的命令示例用代码块展示，不要用 Shell 工具执行

### 严禁

- **禁止通过 Shell 工具运行 `deploy/cli.sh`**
- **禁止通过 Shell 工具运行 `docker build`、`docker push`**
- **禁止通过 Shell 工具运行 `kubectl set image`、`kubectl rollout`**
- **禁止以任何方式自动触发构建或部署流程**

## 变更规模判断规则

**当需要改动的内容涉及 ≥1 个独立功能点时，必须提示用户进入 Plan 模式。**

- "功能点"指的是：新增 API、新增页面/组件、修改数据模型、新增服务层逻辑、涉及多文件联动修改等
- 单纯的文案修改、样式微调、删除一行代码等不算独立功能点
- 提示话术示例：`"这个需求涉及 X 个功能点（xxx、xxx），建议进入 Plan 模式来系统性规划，要不要我帮你创建？"`
- 用户明确拒绝后可以继续在当前模式执行
