---
description: Plan 必须包含前端表现层变化摘要，让用户无需阅读代码即可理解改动影响
alwaysApply: true
---

# Plan 前端表现变化摘要

## 规则

**每个 Plan 必须包含「前端表现变化」章节**，从用户可感知的黑盒视角描述 UI 层面会发生什么变化。

用户不应被迫阅读每一行代码改动才能理解 plan 做了什么。这个章节用最直白的语言告诉用户：哪个页面、哪个元素、从什么样变成什么样。

## 格式

在 plan 输出中新增 `### 前端表现变化` 章节，放在改动步骤之前（让用户先建立直觉再看技术细节）。

每条变化用以下结构：

```
### 前端表现变化

- **[页面/位置]**：[原来是什么] -> [变成什么]
```

## 示例

```
### 前端表现变化

- **Portal 登录页**：验证码登录 Tab 原本隐藏 -> 现在默认可见
- **Portal 登录页**：底部「手机号未注册将自动创建账户」提示文案 -> 移除
- **Admin 系统设置页**：新增「SMTP 邮件配置」卡片，包含服务器地址、端口、发件人等表单项
- **Portal 组织设置页**：CE 版下新增「邮件配置」Tab，功能与 Admin 版一致
- **实例详情页**：顶部面包屑「OpenClaw」-> 重命名为「工作空间」
```

## 适用范围

- 涉及前端改动的 plan：必须写，逐条列出
- 纯后端/基础设施 plan（无任何前端变化）：写「本次改动无前端表现变化」，一行即可
- 修复类 plan（如消息发不出去）：描述修复前后的用户体验差异（如「Agent 发送消息后对方实际收不到 -> 修复后消息正常投递」）

## 禁止

- 禁止用技术术语替代用户视角描述（写「登录页新增验证码 Tab」，不要写「LoginView.vue 新增 TabsTrigger 组件」）
- 禁止省略此章节
